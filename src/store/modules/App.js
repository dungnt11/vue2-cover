const state = {
  heightTitle: 0,
};

const mutations = {
  heightTitle(state, heightTitle) {
    state.heightTitle = heightTitle;
  },
};

const actions = {};

const getters = {};

export default {
  state,
  mutations,
  actions,
  getters,
};
